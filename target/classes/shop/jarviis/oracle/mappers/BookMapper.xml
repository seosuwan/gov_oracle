<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.jarviis.oracle.mappers.BookMapper">
<select id="findAll" resultType="shop.jarviis.oracle.book.domain.BookDto">
select
    book_id bookId,
    book_title bookTitle,
    price,
    pub_id pubId
from books
</select>
<select id="findById" resultType="shop.jarviis.oracle.book.domain.BookDto" parameterType="int">
select
    book_id bookId,
    book_title bookTitle,
    price,
    pub_id pubId
from books
where book_id = ${bookId}
</select>
<select id="findByTitle" resultType="shop.jarviis.oracle.book.domain.BookDto" parameterType="string">
select
    book_id bookId,
    book_title bookTitle,
    price,
    pub_id pubId
from books
where book_title like '%${value}%'

</select>
<select id="findByPrice" resultType="shop.jarviis.oracle.book.domain.BookDto" parameterType="int">
select
    book_id bookId,
    book_title bookTitle,
    price,
    pub_id pubId
from books
where price > ${price}
</select>
<select id="findByPubId" resultType="shop.jarviis.oracle.book.domain.BookDto" parameterType="int">
select
    book_id bookId,
    book_title bookTitle,
    price,
    pub_id pubId
from books
where pub_id = ${pubId}
</select>
</mapper>

